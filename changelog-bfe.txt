== 07-07-2026 - 0.8834 !!!Experimental!!! ==
* Replace the About dialog contributor thanks block with build, runtime, OS, memory, compiler and bundled-library diagnostics
* Fix an About dialog crash in optimized builds when the bundled GeoIP library reports no version string
* Replace hash database persistence with bundled SQLite storage while preserving existing sharing, search, queue and TTH lookup behavior
* Migrate existing HashIndex.xml/HashData.dat data into HashStore.sqlite3 on first startup, record migration completion metadata, rename migrated legacy files and log invalid, corrupt or orphaned hash records during load, migration and rebuild operations
* Add SQLite hash database batching, statement reuse, schema migration, foreign-key cleanup, integrity checks, optimize/compact actions and Sharing-page maintenance settings
* Add unit coverage for SQLite hash persistence, stale hash cleanup, rebuild pruning, legacy migration, schema migration, batching, compacting and corrupt-row handling
* Add hash-store startup benchmarks and reduce hash/share startup overhead for large shared file sets
* Add a validated SQLite share-tree startup cache with background refresh to avoid blocking startup on large shared directory scans
* Verify requested uploads by TTH during share refreshes, retry alternate shared paths for moved files and cache verification results for unchanged files before returning a retryable upload-queue response
* Clear stale ADC hub user state and hub-frame user lists when reconnecting to prevent duplicate self entries with reused CIDs and new SIDs
* Fix RichTextBox context-menu overload visibility to avoid getMenu overloaded-virtual warnings
* Add an Experimental setting to cap RichEdit auto-detected link lengths and ignore overlong chat links
* Ensure file-list, private-message and download connection attempts use the UI-initiating hub's hinted user
* Preserve exact share-cache real paths, skip startup share-cache loading for UNC share roots and normalize legacy hash-database UNC paths
* Fix share-cache schema upgrades reporting database table locks while replacing older cache files
* Clear stale download connection association state before removing redundant queue items to prevent ConnectionManager failure assertions
* Restore the primary hub identity after failover so manual and automatic reconnects retry the configured hub and retain standard hub hints and favorite settings
* Harden nonblocking socket reconnects after network sleep/resume, preserve pre-connect buffer options and establish SOCKS tunnels before TLS
* Wake buffered-socket task dispatch immediately instead of polling idle sockets every 250 milliseconds
* Harden QueueManager large-queue handling and index queued files by TTH to avoid invalid iterators and full scans with massive queues

== 07-04-2026 - 0.8833 !!!Experimental!!! ==
* Advertise enabled IPv4 and IPv6 ADC connectivity independently of optional HBRI hub support
* Update CPMI typing and message-seen state from tab visibility instead of obsolete MDI activation messages
* Add asynchronous ADC HBRI validation for IPv4 and IPv6 connectivity through hubs on the other address family, based on the AirDC++ implementation
* Drain surviving hub clients before buffered socket shutdown to prevent shutdown hangs
* Add independent font, text color and background color customization for every chat style
* Add ADC MCN1 multi-connection transfers with per-user download and upload limits
* Centralize transfer flags and display segmented downloads and uploads with the C flag
* Add a documented Experimental settings page with visible defaults for protocol queues, peer connections, flood control, UDP/SUDP and generated partial-list limits
* Bound queued protocol output, compressed command batches and incomplete commands to prevent excessive memory use
* Make shared flags, connection activity, search timing and shutdown state thread-safe
* Keep connection tokens unique and rework listener and connection-queue notifications to avoid callbacks under internal locks and wait safely for in-flight listeners
* Enforce a global peer-connection cap and per-address incoming connection flood windows before starting TLS handshakes
* Fix ADCS login stalls caused by waiting on the wrong TLS readiness event and rejecting valid post-authentication BINF commands
* Validate ADC parameter lengths, SIDs, CIDs and connection identities before decoding or using them
* Restrict hub-directed client, UDP, HBRI and failover endpoints to valid protocols, ports and safe public or private/local hub scopes
* Improve SOCKS5 validation, support IPv4, IPv6 and domain reply addresses, and apply cumulative read and write timeouts
* Rate-limit incoming UDP packets globally and per peer, cap tracked peers and verify search-result source addresses
* Harden SUDP encryption and decryption against malformed packets, invalid keys, random-number or cipher failures and plaintext downgrade
* Bound retained SUDP search keys and reject malformed ADC and NMDC search results, hashes, sizes and slot counts
* Recognize plaintext NMDC and ADC UDP packets before attempting SUDP decryption
* Prevent MCN1 from opening duplicate full or partial file-list transfers and remove stale pending or idle connections when a list starts
* Prevent delayed connection events from leaving duplicate blank rows in TransferView when a download starts
* Avoid redundant ZL1 wrapping for BZip2 file lists, validate completed list signatures and discard corrupt list downloads
* Bound generated partial-list XML, reject invalid tree and partial-list ranges and validate ADC GET parameters
* Handle recoverable ADC transfer errors without falling through into success handling
* Synchronize upload slot accounting and prevent slot counters from becoming negative
* Fix a TransferView user-command submenu crash and use the correct user-command context
* Correct IPv4 and IPv6 public, private and link-local classification, including scoped IPv6 addresses, and disable unusable auto-detected IPv6 connectivity
* Make string trimming independent of the installed system locale
* Fix opening the configured downloads directory from the main window
* Restore full DWARF function prototypes and improve crash-log symbol resolution
* Fix dangling public-hub XML attribute references while loading hub lists
* Return a valid empty file list for favorite hubs with an explicit empty share profile, including failover connections
* Fix strict-aliasing and offsets beyond 4 GiB in unbuffered Windows file reads
* Generate context-help headers with correct CRLF line endings on Windows
* Fix toolbar reconnect causing hubs to enter an immediate reconnect loop
* Expand ADC, SUDP, HBRI and network endpoint validation tests

== 07-04-2026 - 0.8832 !!!Experimental!!! ==
* Add semantic RichEdit chat coloring for timestamps, nicknames, message text, system messages and mentions
* Add separate timestamp, nickname and message colors for your own outgoing chat messages
* Batch RichEdit chat updates to reduce UI pressure while preserving selection and scroll position
* Harden RichEdit text limits, trimming, range handling and embedded-image insertion
* Add configurable chat emoticons loaded from .dcemo ZIP packages
* Add XML shortcut rules and BMP, ICO and PNG image support for emoticon packages
* Add an Appearance > Emoticons settings page with package, display-size and preferred bit-depth selectors
* Add a settings-based .dcemo package builder and exporter with package versions inherited from VERSIONSTRING
* Add generic XML emoticon-package importing and conversion to .dcemo
* Prefer 16-bpp or higher ICO frames and render full-color PNG images in RichEdit
* Add archive extraction path validation for imported packages
* Add chat-format tests for emoticon boundaries, disabling emoticons and hub-specific mentions

== 06-28-2026 - 0.8831 !!!Experimental!!! ==
* Current DC++ HG Rev [9ece08] (as of 06-21-2026)
* Add CPMI notifications for direct private message channels (typing, message seen and coordinated window closing)
* DWARF5 support for MinGW-w64 builds
* Add support for DMSG in ADC
* Add FailOver support for ADC
* Separate CCPM logic from MainWindow and PrivateFrame into PrivateChatManager
* Statically link libpwinthread
* Lazy load SettingsDialog pages to increase responsiveness
* OpenSSL wrapper for MinGW-w64 compilers built with UCRT (OpenSSL libs are compiled with msvcrt)
* Decrease UI pressure from TransferView by grouping updates into one UI callback
* Add Per-Fav Hub shares
* Remove boost 
* Boost::tribool is replaced with a generic implementation
* Use DWTLib's copy of IntrusivePtr to replace Boost::intrusive_ptr
* This project makes use of ChatGPT Codex 5.5 with High Reasoning for debugging, code checking, and modifying scripts along with porting code
